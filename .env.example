DATABASE_URL=postgresql+asyncpg://devkit:devkit@localhost:5432/devkit
REDIS_URL=redis://localhost:6379

GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GITHUB_APP_PRIVATE_KEY=

GEMINI_API_KEY=
# Model names are optional — defaults are set in config.py.
# Override here when Google deprecates a model without requiring a code change.
# GEMINI_EMBEDDING_MODEL=gemini-embedding-001
# GEMINI_GENERATION_MODEL=gemini-3.5-flash

JWT_SECRET=change-me-to-a-long-random-string
# base64-encoded 32-byte Fernet key
# generate: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=

FRONTEND_URL=http://localhost:5173
ENVIRONMENT=development
